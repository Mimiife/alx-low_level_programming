#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * malloc_checked - allocate malloc memory
 * @b: size of pointer
 * Returns: ponter to the allocate memory
 *
 */

void *point;
point = malloc(b);

if (point == NULL)
	exit(98);

	Return (point);


